#!/usr/bin/env coffee

###
	//
	//	start
	//	seed.bin
	//
###

seeds = require '../lib/seed'
http  = require 'http'

# debug

###

	debugging 

###
	
walk = (base, indent = "", prop = "id") ->
	if base[prop]
		console.log "#{indent}#{base[prop]}"
		walk base.children[id], "#{indent}  ", prop for id of base.children


# seed creation:
dev    = new seeds.Seed "dev/webroot", "^\\/development\\/"
prod   = new seeds.Seed "prod/webroot", "^\\/production\\/"


# binding to http.Server
#server = new http.Server(); server.listen 8080
#server.on 'request', (req, resp) -> dev.request req, resp
req1 = "/development/index.html"
req2 = "/development/static/css/standard.css"
req3 = "/development/dynamic/source/stylus/_binder.styl"
req4 = "/development/static/js/include.js"
req5 = "/development/dynamic/source/jade/index.jade"
req2 = "/development/info.html"

dev.request url: req1